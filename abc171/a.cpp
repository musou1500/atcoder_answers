#include <bits/stdc++.h>

using namespace std;

int main(int argc, const char *argv[]) {
  char c;
  cin >> c;
  if (c >= 'a' && c <= 'z') {
    cout << 'a' << '\n';
  } else {
    cout << 'A' << '\n';
  }

  return 0;
}
