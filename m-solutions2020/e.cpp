#include <bits/stdc++.h>

using namespace std;

int main(int argc, const char *argv[]) {
  int max_c = 2000001;
  vector<vector<int>> vrx(max_c), vry(max_c), vlx(max_c), vly(max_c),
      vdx(max_c), vdy(max_c), vux(max_c), vuy(max_c);
  int n;
  cin >> n;

  for (int i = 0; i < n; ++i) {
    int x, y;
    char u;
    cin >> x >> y >> u;
    if (u == 'u') {
    }
  }
  return 0;
}
